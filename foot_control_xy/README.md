# foot_control_xy

1. run /dev/init.sh to set up the model path in gazebo. Make sure that the package is in ~/catkin_ws/src/ 


Example of Way of launching:

$ roslaunch foot_control_xy visualization.rviz.launch enableProtocol:=true enableJoystick:=true enableGazebo:=false
